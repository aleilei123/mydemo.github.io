(function(){
$drag=$('#drag');
var dragweidth=$('drag').style.left;
function centerdrag(id){
var windouwidth=document.documentElement.clientWidth;
var windowheight=document.documentElement.clientHeight;

 // var dragweidth=$(id).style.
}
})